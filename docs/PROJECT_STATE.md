# Состояние проекта

Обновлено 23.09.2026: сайт опубликован на Vercel Hobby, каталог подключён к Supabase Free.

## Последняя проверка: объединение всех веток и отладка

- История обеих веток `codex/*` объединена в main: merge-коммиты 4a86d6a и 4fc534a. Рабочий frontend сохранён; автономный прототип находится в `prototypes/offline-web/`, мобильные концепты — в `future-app-figma/`. Архивы исключены из deployment.
- В начало README добавлена ссылка на демо и сценарий для посетителя GitHub. Поле About → Website недоступно для изменения текущему аккаунту (WRITE, запрос GitHub API вернул 404).
- Исправлены устаревшие результаты при редактировании, проверка неполных ответов API, сохранение категории, клавиатурное переключение вкладок и тест URL JS с версией. Добавлены лимит JSON 64 КиБ (413), предел безопасного integer бюджета и allowlist публичных static-файлов.
- Актуальный результат: **70 Python-тестов и 19 JS-тестов**, в том числе 264 сценария на исходных 66 профилях. Прототип: 144 сценария/94 альтернативы. Контракт, TypeScript strict, doctor, demo, pip check прошли; pip-audit проверил 16 зависимостей, известных уязвимостей не найдено.
- Реальный локальный HTTP: 40 запросов при параллельности 8, одинаковые карточки, независимые request_id, ошибок нет. Браузер: три статуса, альтернативы, мобильная ширина, изменение формы при задержанном ответе, timeout, повреждённый ответ, 500 и восстановление после 503.
- [Полный отчёт и команды проверки](DEBUG_REPORT.md). Сведения ниже о прежних 58/65 тестах относятся к предыдущим этапам; актуальная проверка перечислена здесь.
- Изменения отправлены в GitHub: PR #2 автоматически отмечен MERGED, обе remote-ветки являются предками main. Код `078a9ed` опубликован на прежний публичный адрес; Vercel deployment `dpl_BMqNrvsWyE7isgL2KJMXviYERLFZ` — READY. Следующий коммит меняет только отчёт о проверке этой версии.
- Новый публичный прогон без авторизации: сайт/JS/health/docs — 200; служебные файлы — 404; некорректный бюджет — 422; тело больше 64 КиБ — 413. Статусы и числа 6/1/0/0 подтверждены, альтернативы дали 3/1. Четыре исходных POST: 195,72–214,03 мс. Браузер production подтвердил 19.12 → 17.12 (1 → 3) без ошибок JS.

## Публичный запуск

- Сайт: https://firebird-six.vercel.app/ ; API-документация: https://firebird-six.vercel.app/docs . Доступ проверен обычным HTTP-клиентом без cookies, токенов и авторизации Vercel.
- Vercel: `airsmooths-projects/firebird`, Hobby. Supabase: организация `Firebird`, проект `firebird`, Free, Mumbai; dashboard https://supabase.com/dashboard/project/icscosicyoaraedvhvgh . Платные планы и GPU не включались.
- Production: `AI_PROVIDER=local`, `RANKING_PROVIDER=tfidf`, `CATALOGUE_PROVIDER=supabase`; серверный ключ хранится как Vercel Secret. Он не записан в исходники или frontend. Live AI по-прежнему не проверен.
- Таблица `public.firebird_contractors` импортирована из исходного CSV. Живая SQL-проверка: 66 строк, 13 synthetic, RLS=true, anon SELECT=false, authenticated SELECT=false, service_role SELECT=true, INSERT=false.
- Backend загружает Supabase через REST и сверяет все поля с исходными 66 профилями. Успешный снимок кэшируется на процесс; ошибка первой загрузки даёт 503 `catalogue_unavailable`, без переключения на CSV. Doctor остаётся offline; экспорт и демо принудительно используют CSV.
- Публичный HTTP: `/health`, HTML и JS — 200; `/.env` и `/data/contractors.csv` — 404. Четыре бизнес-запроса дали 6 / 1 / 0 / 0 подходящих, статусы matched / matched / category_absent / no_eligible. Применение 17.12 вместо 19.12 дало 3 подходящих. Замеры этих POST: 0,19–0,33 с в одном прогоне, не гарантия задержки или холодного старта.
- В реальном браузере публичная форма загрузила справочник, показала Кики на 19.12; кнопка альтернативы изменила дату на 17.12 и показала три карточки.
- После объединения с новым локальным запуском команды: 65 Python-тестов, 14 Node-тестов; экспорт контракта актуален, TypeScript strict прошёл. Семь новых тестов проверяют Supabase, повреждённые/лишние строки, безопасные ошибки, восстановление после сбоя, offline doctor и закрытые пути fullstack-сервера.
- Первый облачный build не принял подключение constraints-файла. Исправлено: requirements.txt и requirements-lock.txt содержат одинаковые плоские списки проверенных версий; повторный build успешен на Python 3.14.
- Приватный репозиторий организации не подключён к Vercel Git integration на Hobby. Публикация выполнена официальным CLI из рабочей копии; следующий push сам по себе сайт не обновит. Повторный запуск: `npx vercel --prod`. [Инструкция](DEPLOYMENT.md), [обзор веток и работ](REPOSITORY_REVIEW.md).

## Что работает

- FastAPI и CLI используют общий pipeline. Исходный CSV: 66 профилей, 13 synthetic; SHA-256 `6a724b6b7dfb5973343e68ba18dadb60fc807d87e3d78f03ee86fb26cb089f7d`, данные не изменялись.
- Общие hard filters в eligibility.py используются и в подборе, и при расчёте альтернатив. Окно 23.09–31.12.2026, цена «от», null часов не ограничивает длительность.
- При 0–2 подходящих — минимальные проверенные одиночные изменения даты, бюджета, часов. Если одного мало — до трёх сочетаний с минимальным числом изменённых полей и явно указанным порядком компромиссов. Каждый вариант содержит воспроизводимый request, число кандидатов и ID.
- understanding показывает ограниченный локальный разбор brief. Однозначный язык применяется, если поле language пусто; конфликт и несколько языков отмечаются. Стиль и отрицания требуют подтверждения.
- Карточки добавляют why_fits, to_clarify, differences; сравнения касаются только показанных кандидатов. Исходные поля ответа сохранены.
- TF-IDF char-tfidf-3-5-v1 — основной режим. RANKING_PROVIDER=brev включает NIM /v1/ranking; AI_PROVIDER=brev независимо включает /v1/chat/completions. Оба только по явной настройке, имена моделей обязательны.
- На внешние этапы общий deadline 6 с, ранжирование максимум 2 с. Ошибка reranker даёт tfidf с ranking_reason, ошибка объяснений — проверенные локальные цитаты с ai_reason. При пустой выдаче внешних запросов нет.
- `AGENTS.md` задаёт карту проекта и правила для Codex; `docs/FRONTEND_HANDOFF.md` — подключение существующего интерфейса и состояния экрана. Ранний автономный прототип web/dist имеет собственный алгоритм; текущий web/ использует HTTP API.
- `integration/` содержит клиент JavaScript без фреймворка, TypeScript-типы, OpenAPI и реальные примеры API. Схема, типы моделей и примеры генерируются `python -m contractor_match.export_contract`; `--check` обнаруживает расхождения. Экспорт всегда local + tfidf.
- Клиент преобразует поля формы, различает бизнес-ответ и ошибку, ограничивает ожидание, поддерживает отмену и предотвращает перезапись свежего результата старым. Он не фильтрует и не ранжирует подрядчиков.
- `requirements-lock.txt` и `requirements.txt` фиксируют одинаковые версии проверенного Python-окружения для воспроизведения API-схемы и сборки Vercel. Обновление обоих файлов требует повторной проверки тестов и экспорта.
- `X-Request-ID` создаётся сервером для каждого HTTP-запроса, включая обработанные ошибки и OPTIONS. `X-Process-Time-Ms` показывает время до начала ответа. Оба заголовка открыты для браузера через CORS и описаны в OpenAPI; тело бизнес-ответа не менялось. Клиент сохраняет полученный номер в ApiClientError.requestId.
- JSON-журнал приложения в stderr связывает HTTP, подбор и fallback одним номером; показывает времена configuration/understanding/catalogue/scope/filters/alternatives/ranking/explanations, счётчики и причины. При 500 — файл/функция/строка без текста исключения, исходных строк, локальных переменных и абсолютных путей. Параллельные запросы имеют независимый контекст.
- `python -m contractor_match doctor [--json] [--frontend-origin ORIGIN]` проверяет Python, зависимости, настройки AI/CORS, свежий CSV и индекс. Работает без site-packages, не вызывает сеть и не подтверждает доступ к моделям. Ошибки дают exit 1, предупреждения — 0. Инструкция: docs/DEBUGGING.md.
- `web/` содержит адаптивную страницу с вертикальной формой, полем пожеланий и вкладками основной подборки и запасных вариантов. Данные формы, карточек и альтернатив берутся из API через общий клиент; тексты API добавляются в DOM как текст. Вторая вкладка отдельно показывает проверенные сервером изменения условий и нюансы пожеланий у уже показанных кандидатов.
- Ошибки загрузки справочника и подбора в web/ показывают код обращения, если общий клиент получил request_id от сервера.

## Исправлено при подготовке фронтенда

- До изменения браузерный OPTIONS preflight получал 405. Теперь CORS допускает точные настроенные origins и методы GET/POST. По умолчанию localhost/127.0.0.1 на портах 3000, 4317, 5173; конфигурация — `CORS_ALLOWED_ORIGINS`, без wildcard и передачи cookies.
- Неожиданный сбой раньше возвращал текстовый 500. Теперь 404/405/422/500/503 используют JSON-envelope `error`; обработанный 500 сохраняет CORS-заголовки и не раскрывает внутренние детали. Изначально лог содержал только класс исключения; в текущей итерации добавлены безопасные координаты сбоя и request_id.
- `.env.local` и другие `.env.*` теперь игнорируются Git вместе с `.env`; примеры разрешены. Добавлены исключения для зависимостей Node и сборки Next.js.

## Проверено

- 58 unittest-тестов локально: прежние 46 + 6 для request_id/логов и 6 для doctor. Проверены 8 параллельных запросов без смешивания контекста, отсутствие личного текста/ключей в логах, безопасные кадры 500, fallback, CLI stdout/stderr и doctor без установленных пакетов. Провайдеры проверяются имитациями без сети/оплаты.
- 14 Node-тестов клиента: прежние 12 + сохранение номера в HTTP/невалидном ответе и timeout чтения тела; до получения ответа номер отсутствует. TypeScript-проверка обновлённых объявлений прошла с `--strict --noEmit --module NodeNext`.
- Обновлённая проверка реального HTTP: Uvicorn + Node-клиент, 6 запросов сопоставлены с JSON-журналом по request_id, CORS expose-заголовки доступны, ApiClientError.requestId для 422 совпадает с номером на сервере. Сценарий 19.12 → 17.12 сохраняет 1 → 3 подходящих. Временный сервер остановлен.
- Реальный HTTP: запущен временный Uvicorn, запросы выполнены готовым Node-клиентом. Preflight с origin localhost:4317 — 200; ведущий 19.12 — 1; применение предложенного 17.12 — 3; строковый бюджет — 422 с полем budget_kzt. Все ответы сохранили нужный CORS-заголовок. Сервер остановлен после проверки. Это проверка транспорта, не готового интерфейса в браузере.
- Реальный CLI: восемь исходных сценариев и отдельная история из пяти запросов. История: 6 → 1 → 3 ведущих; 0 → 1 флорист при двух изменениях.
- Замер CLI в отдельном процессе на Windows/Python 3.14.7: исходные сценарии 0,09–81,66 мс; история 0,93–100,28 мс, включая холодный индекс.
- Реальный Uvicorn: четыре параллельных POST дали одинаковые ответы за 41,60–52,66 мс; повтор POST с предложенной датой 17.12 подтвердил 3 подходящих вместо 1.
- Общий deadline двух NIM-этапов проверен с уменьшенным временем и отменой зависшей chat-coroutine. Ранее полный имитированный timeout 6 с давал ответ за 6,041 с.
- GitHub Actions удалён в параллельных изменениях команды; это решение сохранено. Проверки выполняются локально без удалённого CI.

## Что пока не подтверждено

- Живой OpenAI, NVIDIA Build и NIM на Brev. По решению пользователя подготовлены только код и инструкция; GPU, купоны, биллинг и Launchable не создавались/не менялись.
- Прирост качества русского семантического ранжирования и стабильность оценок на конкретной модели/GPU. Порядок при смене модели или fallback может отличаться; по одинаковым оценкам действует цена/id.
- Полное понимание произвольных пожеланий: локальные правила и сходство текста этого не гарантируют. Бронирования и живого обновления календаря нет.
- Нагрузочное поведение и длительная доступность на бесплатных облачных квотах. Публичный функциональный прогон выполнен, но не является нагрузочным тестом. Мобильная ширина ранее проверялась локально на примерах контракта; живой AI не проверен.
- В API нет полного досье, контактов, списка исключённых профилей и отдельной выдачи кандидатов при ослаблении пожеланий. Пояснения по пожеланиям относятся к уже показанным кандидатам; новые варианты такого рода потребуют расширения сервера. Клиент проверяет базовую форму ответа, а не всю JSON Schema во время выполнения.

## Следующий шаг

Команде: использовать публичный сайт или локальный run_web.py. После изменения main выполнить проверки и повторить CLI deploy согласно DEPLOYMENT.md. Ошибки искать по коду обращения в журнале согласно DEBUGGING.md. Дизайн развивать поверх действующего HTTP-клиента; мобильные концепты описаны в REPOSITORY_REVIEW.md.

После предоставления адреса уже настроенного NIM — отдельный живой прогон по docs/BREV.md, сравнение релевантности с TF-IDF на тех же запросах и только затем выбор основного режима для защиты.

## Локальный сайт с подключённым API — 2026-09-23

- Добавлен `run_web.py`: FastAPI раздаёт `web/`, общий клиент из `integration/` и текущие маршруты API на одном локальном адресе. `start-web.cmd` устанавливает зависимости и запускает страницу в браузере на Windows.
- Страница по умолчанию обращается к тому же origin, откуда загружена; адрес API меняется вместе с портом сервера. Для отдельного хоста по-прежнему можно задать `EVENT_API_BASE_URL`.
- Проверено локально с реальным Uvicorn: `/health` — 200, `/catalogue/options` — 3 города, POST `/recommendations` по сценарию Алматы/Ведущий — 200 и 3 карточки. Страница загружена в браузере с данными справочника, полученными с API.
- В текущем окружении нет настроенных ключей OpenAI, NVIDIA или Brev. Проверка выполнена в локальном режиме, без внешнего AI вызова и без GitHub Actions.

## Детали карточки и итоги подбора — 2026-09-23

- Раскрывающийся блок карточки переименован в «Подробнее» и содержит отдельные разделы: описание (доступный фрагмент из каталога), почему подходит именно вам, что уточнить у подрядчика и отличия среди показанных.
- Счётчики и причины из структурированного ответа API показаны списком под карточками. Счётчики причин могут пересекаться; интерфейс сохраняет эту оговорку.
- Проверено в живом локальном браузере на запросе Астана / Фотограф / 22.11.2026: одна карточка, четыре раздела при раскрытии, список итогов под ней, две причины несовпадения по формату.

